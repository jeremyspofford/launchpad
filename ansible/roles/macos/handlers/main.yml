---
# ============================================================================ #
# macOS Handlers
# ============================================================================ #
# Handlers to restart system services when configuration changes
# ============================================================================ #

- name: restart_dock
  ansible.builtin.command: killall Dock
  failed_when: false

- name: restart_finder
  ansible.builtin.command: killall Finder  
  failed_when: false